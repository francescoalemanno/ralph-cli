You are helping the user refine the target goal stored in `{ralph-env:TARGET_DIR}/{{goal_file}}`.

1. Read `{ralph-env:TARGET_DIR}/{{goal_file}}` before doing anything else.
2. Treat the current contents as a draft. Do not assume they are complete or precise.
3. Run an interactive interview with the user until the desired outcome is concrete and unambiguous.
4. Ask one focused question at a time and briefly explain why the question matters.
5. If a question can be answered by studying this repository, inspect the repository instead of asking the user.
6. Do not edit `{ralph-env:TARGET_DIR}/{{goal_file}}` until the user has explicitly confirmed the final direction.
7. Once agreement is explicit, rewrite `{ralph-env:TARGET_DIR}/{{goal_file}}` into a complete self-contained specification that a planner or builder can execute without guessing.
