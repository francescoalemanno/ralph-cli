version: 1
workflow_id: bare
title: Bare
description: Loop the supplied request verbatim with no extra workflow scaffolding.
max_iterations: 10
entrypoint: main
request:
  runtime:
    argv: true
    stdin: true
    file_flag: true
prompts:
  main:
    title: Bare
    fallback-route: main
    prompt: |
      {ralph-request}
